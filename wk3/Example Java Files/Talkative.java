public interface Talkative // interface
{
	void Say(String message); // "abstract" method (no body)
	// all interface methods are public abstract by default
	// they must be fully defined by any non-abstract implementing class
}